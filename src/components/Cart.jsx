import React from 'react';
import { getStoredCart } from '../utils/fakeDB';

const Cart = () => {
    const savedCart = getStoredCart()

    return (
        <div>

        </div>
    );
};

export default Cart;